{
    "python.formatting.provider": "black",
    "python.linting.flake8Enabled": true,
    "markdown.validate.ignoredLinks": [
        "{{ project.url }}",
        "{{ project.contributions_url }}"
    ]
}
